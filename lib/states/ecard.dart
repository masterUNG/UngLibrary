import 'package:flutter/material.dart';

class Ecard extends StatelessWidget {
  const Ecard({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text('Ecard'));
  }
}
